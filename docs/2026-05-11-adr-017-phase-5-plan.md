---
title: "ADR-017: Phase 5 (Organization Engine) plan"
type: decision
status: "Proposed — pending Thad sign-off on DECISION blocks"
date: 2026-05-11
---

## Context

Phase 4 (Write Layer) shipped as v0.5.0 end-to-end working. All 9 spec §3.4 write tools are live with diff-first approval, per-turn transaction log, conflict detection, and an undo command. The next phase per the [CLAUDE.md phase map](../CLAUDE.md) is:

> 5 — Organization engine | auto-routing, MOC maintenance | future

**Different mental model than Phase 4.** Phase 4 was *reactive* — the user asks, the agent does, every action gates through a diff card. Phase 5 is *proactive* — the agent observes the vault, detects opportunities to improve structure, and *suggests* actions. The user batches accept/reject when they have time, not when the agent acts.

This ADR locks the design plan for Phase 5, the new infrastructure required (event watcher, suggestion queue, classifier service), and the rollout sequence from v0.6.0 (MVP) through v0.7.0 (Phase 5 close).

**Version-map note:** The CLAUDE.md phase map originally said "5 — Organization engine = v0.5." That target collided with Phase 4 close (which we shipped *as* v0.5.0). Phase 5 retargets to **v0.6.x → v0.7.0**; CLAUDE.md is updated accordingly when this ADR is accepted.

Per [ADR-010 §4](2026-05-04-sagittarius-build-process.md), no Phase 5 code lands before this ADR is signed off.

---

## Decisions

### D1 — Trigger model

**Hybrid: vault events for create + manual sweep + a "check inbox now" command.**

```
Obsidian `vault.on('create', file)` fires → if file is in a watched folder,
  queue an organize-task for that file (debounced 5s after the user stops typing).

User runs `Sagittarius: organize inbox now` →
  scan every file in watched folders that isn't already in the suggestion queue.

[Optional] Background sweep every N minutes (default off; user opt-in via setting).
```

**Rationale:**
- Events are responsive — new note created → suggestion appears in seconds.
- Manual sweep handles the catch-up case (e.g., user pasted 20 notes from another app at once).
- Background sweep is opt-in because most users don't want a periodic process running.
- 5s debounce prevents thrashing on rapid-fire edits to the same file.

**Rejected:**
- *Pure event-driven*: misses the catch-up case.
- *Pure periodic*: feels laggy for fresh notes.
- *Manual only*: defeats the "proactive" framing of the phase.

`<DECISION D1: accept hybrid event + manual sweep + opt-in periodic, OR override>`

---

### D2 — Suggestion UX surface

**Dedicated sidebar panel: "Sagittarius — Suggestions" (`SuggestionsView`). Separate from the chat panel.**

UI structure:
- One row per pending suggestion: `[icon] description  [Apply] [Skip] [Defer]`
- Description shows the proposed action in plain English: "Move `10-Inbox/Hangar-meeting-2026-05-11.md` to `70-Memory/meetings/`. Reason: 4 of 5 similar notes live there."
- `Apply` runs the proposed write through the standard diff card from Phase 4 (so the user still confirms).
- `Skip` removes the suggestion from the queue without applying.
- `Defer` keeps it in the queue but moves it to the bottom.
- Header shows count of pending + a `Clear all` action.

**Why a separate panel, not chat-card or notice:**
- Chat-card was designed for 1:1 reactive flow (user asks → agent proposes). Mixing proactive suggestions in there drowns conversation.
- Notice popups are too intrusive — N suggestions could mean N popups firing in rapid succession.
- A panel matches the user's mental model: "the queue of things Sagittarius noticed."
- Ribbon icon (lightbulb or list) with a badge for unread suggestions provides ambient awareness without modal interruption.

**Rejected:**
- *Notices*: rejected per ADR-016 D2 (same reasoning).
- *Chat-card embed*: mixes mental models.
- *Settings-tab list*: too buried; users won't check.

`<DECISION D2: accept dedicated panel + ribbon badge, OR override>`

---

### D3 — Suggestion types for v0.6.x

**v0.6.x ships two suggestion kinds; later versions extend.**

1. **`route` — folder routing for inbox notes.**
   - Inputs: a note's path + body + frontmatter.
   - Decision: should this note stay in `10-Inbox/` or move to a more-permanent folder?
   - Apply: routes through `move_note` (which uses Obsidian's `fileManager.renameFile` so wikilinks auto-update).

2. **`moc-add` — add a note to an existing Map-of-Content note.**
   - Inputs: a note path + the path of a MOC that might want it.
   - Decision: is the new note related-enough to belong on this MOC?
   - Apply: routes through `link_notes` (insert a `[[note]]` under the MOC's anchor section).

**v0.7.0 may extend with:**
- `frontmatter-suggest` (add common frontmatter fields based on note shape)
- `moc-create` (detect cluster of related notes with no MOC, suggest creating one)
- `tag-suggest` (propose tags based on similar notes)

`<DECISION D3: accept route + moc-add for v0.6.x, OR override>`

---

### D4 — Routing intelligence

**LLM-as-classifier with retrieval-grounded context.**

For each `route` suggestion:

```ts
// In OrganizationClassifier service (new):
1. Read the note's content + frontmatter
2. Use the existing RetrievalLayer to find K=5 most-similar existing notes
3. Compose a short prompt:
   "Note title: <basename>
    Note body (first 500 chars): <body>
    Similar existing notes and their folders:
    - 70-Memory/meetings/foo.md (similarity 0.84)
    - 70-Memory/meetings/bar.md (similarity 0.79)
    - 21-Agents/concierge.md (similarity 0.61)
    ...
    Where should this note live? Reply with ONE folder path or KEEP."
4. Call Claude (small model like Haiku for cost) with the prompt
5. Parse response → produce a Suggestion
```

**Why LLM over rules:**
- The vault structure is bespoke per-user; no fixed rule schema will fit.
- Claude is good at noticing thematic clusters from a small retrieval sample.
- The agent can include the user's THAD_MAN.md constitution in the system prompt for context.

**Cost mitigation:**
- Default to Haiku (cheap) — set in `organizationClassifierModel` setting, can override.
- Only run for notes in watched folders.
- Don't re-suggest the same note within N hours.

**Confidence threshold:**
- Classifier emits a confidence score (0..1) — Claude is asked to also return one in its JSON response.
- Only surface suggestions with confidence ≥ `organizationMinConfidence` (default 0.6).
- Below threshold → suggestion stored but hidden behind a "show low-confidence" toggle.

**Rejected:**
- *Pure rules / regex*: too brittle.
- *Local ML classifier*: too heavy for the plugin bundle.
- *Frontmatter-only*: works for notes that already have tags; useless for raw inbox dumps.

`<DECISION D4: accept LLM-classifier + retrieval + Haiku default + 0.6 threshold, OR override>`

---

### D5 — User control / settings

**New settings (extending `SagittariusSettings`):**

```ts
// Phase 5 organization engine
organizationEnabled: boolean;            // default false — opt-in. Won't watch until user flips this.
organizationWatchedFolders: string[];    // default ['10-Inbox/']
organizationClassifierModel:             // default 'claude-haiku-4-5-20251001'
  'claude-haiku-4-5-20251001' | 'claude-sonnet-4-6' | 'claude-opus-4-7';
organizationMinConfidence: number;       // 0..1, default 0.6 — threshold for surfacing in queue
organizationSweepIntervalSec: number;    // default 0 (manual only); 3600 = hourly background sweep
organizationMocFolders: string[];        // folders where MOCs live (e.g., '22-Decisions/'); empty disables moc-add
```

**Defaults rationale:**
- `organizationEnabled: false` — Phase 5 is a meaningful behavior change. Don't opt users in silently.
- Watch only `10-Inbox/` by default — that's where unstructured drafts land.
- Haiku default — keeps classifier cost negligible (~$0.0001 per note).
- 0.6 threshold — biased toward surfacing more suggestions than fewer; users can tighten.
- Sweep off by default — explicit "organize inbox now" command is enough for most.

**Settings UI:**
- New "Organization (Phase 5)" section in the settings tab.
- Toggle for `organizationEnabled` — when off, no watcher fires, no classifier calls happen, no queue items get added.
- All other fields are inputs (text / dropdown / number).

`<DECISION D5: accept settings shape + defaults, OR override>`

---

### D6 — Rollout slicing

**Three-version slice through v0.7.0 (Phase 5 close):**

| Version | Scope | Why first/last |
|---|---|---|
| **v0.6.0 — Foundation MVP** | `SuggestionQueue` + `SuggestionsView` panel + `OrganizationClassifier` (LLM-backed) + `OrganizationWatcher` (vault event hook) + `Sagittarius: organize inbox now` command + `route` suggestion type only. Settings UI for opt-in + watched folders + classifier model + threshold. | Validates the whole event → classify → queue → apply pipeline against the simplest suggestion type (folder routing). Doesn't touch MOCs yet. |
| **v0.6.x — MOC maintenance** | `moc-add` suggestion type. MOC detection (find notes that look like MOCs based on shape — heading + bullet-list-of-wikilinks pattern). `organizationMocFolders` setting wired. | Builds on v0.6.0's queue + classifier. Same UX, new suggestion kind. |
| **v0.7.0 — Phase 5 close** | Optional background sweep (periodic). Bulk operations (apply-all / skip-all in queue). Status-bar indicator. `frontmatter-suggest` if there's time. Settings polish. Phase 5 retrospective ADR. | Closes the loop with the proactive-sweep functionality + UX polish. |

**Prereqs (must land in v0.6.0 PR 1):**
- New `SuggestionQueue` persistence (similar shape to `TransactionLog`: JSON file at `.obsidian/plugins/.../suggestions.json`).
- `OrganizationClassifier` service that wraps the Anthropic SDK with the routing prompt.
- New `Suggestion` type discriminated union.

`<DECISION D6: accept three-version slice, OR override>`

---

## Tool surface (binding)

Phase 5 doesn't add new agent-callable tools. The classifier runs **outside** the ConduitAgent loop — it uses Anthropic's SDK directly with a fixed prompt. The agent's tool registry is unchanged from v0.5.0.

The user-facing surface is:
- Command palette: `Sagittarius: organize inbox now` — runs sweep + classifier
- Command palette: `Sagittarius: open suggestions panel` — opens the SuggestionsView
- Ribbon icon (lightbulb) with badge for pending count
- The suggestions panel itself, with Apply/Skip/Defer per row

`<DECISION D7: accept no-new-agent-tools + classifier-outside-loop, OR override>`

---

## Architecture sketch

```
                        ┌─────────────────────────────────┐
                        │  OrganizationWatcher (new)      │
                        │  - subscribes to vault events   │
                        │  - debounces                    │
                        │  - filters by watched folders   │
                        └──────────┬──────────────────────┘
                                   │ (file path)
                                   ▼
                        ┌─────────────────────────────────┐
                        │  OrganizationClassifier (new)   │
                        │  - reads note via VaultAdapter  │
                        │  - queries RetrievalLayer       │
                        │  - calls Anthropic.messages     │
                        │    with fixed routing prompt    │
                        │  - parses response → Suggestion │
                        └──────────┬──────────────────────┘
                                   │ (Suggestion)
                                   ▼
                        ┌─────────────────────────────────┐
                        │  SuggestionQueue (new)          │
                        │  - JSON-persistent (.../suggestions.json) │
                        │  - dedup by note path           │
                        │  - cap at e.g. 200              │
                        └──────────┬──────────────────────┘
                                   │ (list)
                                   ▼
                        ┌─────────────────────────────────┐
                        │  SuggestionsView (new, sidebar) │
                        │  - render rows                  │
                        │  - Apply → routes through       │
                        │    Phase 4 write tools          │
                        │    (ConduitAgent.chat() with a  │
                        │     synthetic user message?)    │
                        │    — see implementation note    │
                        │  - Skip/Defer mutate the queue  │
                        └─────────────────────────────────┘
```

**Implementation note on Apply.** Two options for wiring "user clicked Apply":
- (a) Call the write tool directly from the panel handler (bypassing the agent). Faster, fewer moving parts, but no LLM in the loop.
- (b) Inject a synthetic user message into the chat agent ("Move foo.md to bar/") and let the normal agent loop handle it. Slower, more cost, but reuses the entire Phase 4 approval pipeline.

`<DECISION D8: (a) direct write — recommended; (b) synthetic-message via agent>`

Recommendation: (a) for performance + simplicity. The diff card from Phase 4 still fires (because all write tools route through `gate.request()`), so the user still confirms before any file changes. The only thing missing in (a) is "Claude narrates what it did" — which the suggestion queue already shows in plain English anyway.

---

## Risks

| Risk | Mitigation |
|---|---|
| Classifier cost spirals on a noisy inbox | Default to Haiku + dedupe by path within N hours + watched folders only |
| User finds suggestions annoying | Opt-in by default; clear "disable organization" toggle |
| Suggestion queue grows unbounded | Cap at 200 entries with FIFO truncation (same pattern as TransactionLog's 1000-cap) |
| Classifier picks wrong folder repeatedly | User can `Skip` to dismiss; pattern of skips on similar notes can feed a future v0.7 "learned skip" feature |
| Vault event noise (rapid edits) | 5s debounce per file |
| Notes outside watched folders accidentally moved | Watcher filters by `organizationWatchedFolders` — only files inside trigger classification |
| Apply triggers a write conflict | Goes through the existing Phase 4 write tools, which already have ConflictDetector + the diff card surfaces conflicts cleanly |
| User accepts a bad move, can't undo | All Apply writes go through the transaction log → `Sagittarius: Undo last write transaction` works as in Phase 4 |

---

## Out of scope (Phase 6+)

- Cross-vault organization (multi-vault routing rules)
- Learning from user skips ("you've skipped 5 similar suggestions; should I stop showing these?")
- Tag autocomplete / tag taxonomy management
- Conversation memory (linking related chat turns to MOCs)
- Cited generative drafts (Phase 8)
- CLAUDE.md reader/writer / dossiers (Phase 9)

---

## Follow-ups (Phase 5 PRs)

- [ ] **PR 1 (v0.6.0):** `SuggestionQueue` + `Suggestion` types + `suggestions.json` persistence + tests.
- [ ] **PR 2 (v0.6.0):** `OrganizationClassifier` service + routing prompt + tests with stubbed Anthropic client.
- [ ] **PR 3 (v0.6.0):** `OrganizationWatcher` + vault event subscription + debounce + tests.
- [ ] **PR 4 (v0.6.0):** `SuggestionsView` UI + commands + settings UI + ribbon icon. End-to-end smoke against Stephen's vault. Ship v0.6.0.
- [ ] **PR 5+ (v0.6.x):** `moc-add` suggestion type + MOC detection + tests.
- [ ] **PR 8+ (v0.7.0):** Background sweep + bulk ops + status bar + Phase 5 close ADR.

---

## Related

- [ADR-016](2026-05-10-adr-016-phase-4-plan.md) — Phase 4 plan, now fully shipped. Phase 5 reuses its write tools.
- [ADR-014](2026-05-09-phase-3-close.md) — Phase 3 close. Retrieval layer is reused by the classifier.
- [ADR-010](2026-05-04-sagittarius-build-process.md) — Pair-via-claude-code. This ADR is the §4 sign-off prerequisite for Phase 5 code.
- [`docs/02_SPEC.md`](02_SPEC.md) §3.4 — write-tool surface that Phase 5 consumes (no new agent-callable tools).
- [`docs/THAD_MAN.md`](THAD_MAN.md) — vault constitution; loaded into classifier system prompt so routing respects user conventions.
